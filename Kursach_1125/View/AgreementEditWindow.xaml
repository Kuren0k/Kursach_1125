<Window x:Class="Kursach_1125.View.AgreementEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mvvm="clr-namespace:Kursach_1125.VM"
        xmlns:local="clr-namespace:Kursach_1125.View"
        mc:Ignorable="d"
        DataContext="{DynamicResource vm}"
        Title="AgreementEditWindow" Height="450" Width="400">
    <Window.Resources>
        <mvvm:AgreementEditWindowMvvm x:Key="vm"/>
        <Style x:Key="Box" TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="0 0 0 30"/>
        </Style>
        <Style x:Key="text" TargetType="TextBlock">
            <Setter Property="Margin" Value="0 0 0 30"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,10,0,0">
            <TextBlock Text="ТПК Зона" Style="{DynamicResource text}"/>
            <TextBlock Text="Арендодатель" Style="{DynamicResource text}"/>
            <TextBlock Text="Дата подписания" Style="{DynamicResource text}"/>
            <TextBlock Text="Дата окончания" Style="{DynamicResource text}"/>
            <TextBlock Text="Арендная плата" Style="{DynamicResource text}"/>
            <TextBlock Text="Статус" Style="{DynamicResource text}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="20,10,20,0" DataContext="{Binding NewAgreement}">
            <ComboBox Height="25" Margin="0 0 0 20"
            ItemsSource="{Binding DataContext.TPKZonesList, RelativeSource={RelativeSource AncestorType=Window}}"
            SelectedItem="{Binding TPKZones}"
            DisplayMemberPath="Title"/>
            <ComboBox Height="25" Margin="0 0 0 26"
            ItemsSource="{Binding DataContext.TentantsList, RelativeSource={RelativeSource AncestorType=Window}}"
            SelectedItem="{Binding Tentants}"
            DisplayMemberPath="Title"/>
            <DatePicker Height="25" Margin="0 0 0 25" Text="{Binding DateOfString}"/>
            <DatePicker Height="25" Margin="0 0 0 24" Text="{Binding EndDate}"/>
            <TextBox Style="{DynamicResource Box}" Text="{Binding RentalRate}"/>
            <CheckBox Content="Действует ли договор" Height="25" IsChecked="{Binding Status}"/>

        </StackPanel>
        <Button Content="Сохранить изменения" Command="{Binding InsertAgreement}" Grid.ColumnSpan="2" Height="40" Width="200" VerticalAlignment="Bottom" Margin="0 0 0 40" FontSize="16" FontWeight="Bold"/>
    </Grid>
</Window>
