<Window x:Class="Kursach_1125.View.AgreementEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursach_1125.View"
        mc:Ignorable="d"
        Title="AgreementEditWindow" Height="450" Width="400">
    <Window.Resources>
        <Style x:Key="Box" TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="0 0 0 30"/>
        </Style>
        <Style x:Key="text" TargetType="TextBlock">
            <Setter Property="Margin" Value="0 0 0 30"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,10,0,0">
            <TextBlock Text="Название кампании" Style="{DynamicResource text}"/>
            <TextBlock Text="Контактное лицо" Style="{DynamicResource text}"/>
            <TextBlock Text="Номер телефона" Style="{DynamicResource text}"/>
            <TextBlock Text="Email" Style="{DynamicResource text}" Margin="0,7,0,35"/>
            <TextBlock Text="ТПК Зона" Style="{DynamicResource text}"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="20,20,20,0">
            <TextBox Style="{DynamicResource Box}"/>
            <TextBox Style="{DynamicResource Box}"/>
            <TextBox Style="{DynamicResource Box}"/>
            <TextBox Style="{DynamicResource Box}"/>
            <ComboBox Height="25"/>
        </StackPanel>
        <Button Content="Сохранить изменения" Grid.ColumnSpan="2" Height="40" Width="200" VerticalAlignment="Bottom" Margin="0 0 0 40" FontSize="16" FontWeight="Bold"/>
    </Grid>
</Window>
