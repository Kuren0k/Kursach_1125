<Window x:Class="Kursach_1125.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursach_1125"
        xmlns:mvvm="clr-namespace:Kursach_1125.VM"
        mc:Ignorable="d"
        DataContext="{DynamicResource vm}"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <mvvm:MainWindowMvvm x:Key="vm"/>
        <Style x:Key="ParentButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0 20 0 0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width ="6*"/>
        </Grid.ColumnDefinitions>

        <StackPanel VerticalAlignment="Top">
            <Button Style="{StaticResource ParentButton}" Content="Главная" Command="{Binding PageNavigation}"/>
            <Button Style="{StaticResource ParentButton}" Content="Финансы" Command="{Binding PageNavigationF}"/>
            <Button Style="{StaticResource ParentButton}" Content="Работники" Command="{Binding PageNavigationEm}"/>
            <Button Style="{StaticResource ParentButton}" Content="Договоры" Command="{Binding PageNavigationAgreement}"/>
            <Button Style="{StaticResource ParentButton}" Content="Арендодатели" Command="{Binding PageNavigationTentant}"/>
            <Button Style="{StaticResource ParentButton}" Content="ТПК Зоны" Command="{Binding PageNavigationTPK}"/>
        </StackPanel>

        <Frame Grid.Column="1" x:Name="frame" NavigationUIVisibility="Hidden"
               BorderBrush="LimeGreen" BorderThickness="2"/>
    </Grid>
</Window>
